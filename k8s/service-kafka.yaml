apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: quant-ai
spec:
  clusterIP: None  # headless service for StatefulSet
  selector:
    app: kafka
  ports:
    - name: client
      port: 9092
      targetPort: 9092
    - name: controller
      port: 9093
      targetPort: 9093
