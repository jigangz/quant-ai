apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: quant-ai
  labels:
    app: quant-ai-api
spec:
  type: NodePort
  selector:
    app: quant-ai-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30001
